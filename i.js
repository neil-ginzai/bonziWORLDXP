const { charset } = require("mime-types");

function newGodmode(){
  var id = (q) => {
    for(i = 0; i < q.length; i++){
      var r1 = Math.floor(Math.random() * charset.length);
      var r2 = r1 + 1;
      var r = charset.substring(Math.floor(Math.random() * charset.length),);
      result+=
    }
  }
  var phrases = ["bonziworld","bonzi","bwxp"];
}